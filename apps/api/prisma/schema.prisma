// STEP 0: datasource/generator 자리만 잡는다.
// 모델·enum·인덱스는 STEP 1에서 docs/DATA_MODEL.md 대로 추가한다.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
