/**
 * 테스트의 고정 "오늘" — ADR-50. **API 프로세스와 브라우저가 같은 값을 봐야 한다.**
 * 두 곳에서 각각 정의하면 언젠가 어긋나고, 어긋나면 F6-1(당일 수정) 판정이 조용히 뒤집힌다.
 * 그래서 여기 한 곳에서만 정한다 — `playwright.config.ts`(API env)와 `fixtures.ts`(브라우저 시계)가 함께 읽는다.
 *
 * 금요일인 이유: 스위트가 쓰는 `days_per_week`(3·4·6)가 **전부 운동일**인 요일이 MON 과 FRI 뿐이고,
 * "내일은 휴식"을 기대하는 화면이 있어 다음 날(SAT)이 휴식이어야 한다 → FRI.
 * 고정하지 않으면 일요일에는 어떤 분할에서도 오늘 세션이 없어 스위트가 통째로 죽는다
 * (실측 2026-08-09 일: 53개 중 17 failed / 22 미실행).
 */
export const TEST_TODAY = process.env.AFC_TEST_TODAY || "2026-08-14";

/**
 * 브라우저 시계를 옮길 기준 시각. 자정을 피해 한낮으로 둔다 —
 * 스위트가 도는 동안 날짜가 넘어가면 그날치 기대값이 흔들린다.
 */
export const TEST_NOW = `${TEST_TODAY}T10:00:00.000Z`;
