# /goal — 프로젝트 자동 진행 & 품질 게이트

> **Claude Code**: `/goal` (이 파일과 동일 내용이 .claude/commands/goal.md에 있음).
> **Codex/기타**: 아래 내용을 그대로 붙여넣어 실행. 인자는 첫 줄에 함께 적으면 된다(예: "until 6").

---

너는 이 저장소(AIFITCOACH 운동 코칭 웹앱)를 STEP 0부터 STEP 8까지 거쳐 **완성**까지 끌고 가는 자율 개발 에이전트다.
스타터 킷의 스펙을 진실의 원천으로 삼고, 각 STEP을 끝낼 때마다 평가·검증 게이트를 통과해야 하며,
최종 완성본은 다각도 평가에서 **99/100 이상**이 되도록 다듬는다.

## 인자(선택): `$ARGUMENTS`
- (없음) → PROGRESS.md 기준으로 마지막 완료 STEP 다음부터 완료까지 진행.
- `until <N>` → STEP N까지만 진행하고 멈춘다(예: 테스트 우선이면 `until 6`).
- `step <N>` → STEP N 하나만 수행.
- `evaluate` → 구현은 건드리지 말고 "최종 다각도 평가"만 수행.

## 시작할 때 항상
1. AGENTS.md, CLAUDE.md, docs/README.md, docs/TEST_SCOPE.md, PROMPTS.md, PROGRESS.md를 읽어라.
2. PROGRESS.md에서 마지막으로 완료된 STEP과 "Deferred(이월) 항목"을 확인하고 이어서 진행하라(재개 가능). PROGRESS.md가 없으면 STEP 0부터.
3. Karpathy 4원칙을 지켜라: 가정을 명시하고, 최소 코드로, 외과적 변경으로, 성공 기준까지 검증한다.
   - 자율 진행 중에는 **되돌릴 수 없거나 보안/결제/개인정보/데이터 손실과 관련된 판단만** 사람에게 멈춰 묻는다.
   - 그 외 사소한 모호함은 가정을 명시하고 진행한다(가정은 PROGRESS.md에 남긴다).

## 팀(서브에이전트) 활용
너는 이 실행에서 **기술총괄(오케스트레이터)** 이다. 각 STEP에서 작업을 독립 하위작업으로 분해하고, `.claude/agents`의 전문 서브에이전트(frontend/backend/ui-ux/design/recommendation/qa)에 **병렬로** 위임하라(같은 파일을 건드리는 작업은 직렬화). 통합 후 **qa**로 게이트를 실행하고 **evaluator**로 스코어카드를 받아라. 상세 배치·핸드오프는 docs/AGENTS_TEAM.md를 따른다.
- 병렬화: 서로 다른 영역의 독립 작업만 동시에 배치하고 자주 통합한다(작은 diff).
- STEP 5는 frontend + ui-ux + design 병렬(+ qa E2E)로 진행하고, 기능이 많으면 frontend 작업을 기능별로 나눈다.
- 최종 99점 루프는 evaluator가 증거 기반으로 채점하고, 최저 항목을 담당 에이전트가 개선 → 재평가한다.
- Codex 등 네이티브 서브에이전트가 없으면 같은 역할·경계·게이트를 순차/병렬 세션으로 적용한다.

## STEP 실행 루프 (STEP N마다 반복)
A. **선언**: 지금 STEP N을 시작한다고 알리고, PROMPTS.md의 해당 STEP 프롬프트와 지정된 docs를 읽어라.
B. **구현**: 그 STEP의 성공 기준을 충족하도록 구현하라. 로직 비중이 큰 STEP(3 추천엔진, 6 동기화)은 **테스트를 먼저** 작성한다.
C. **평가·검증 게이트**: 아래를 실제로 실행하고 로그를 남겨라(말로만 "통과"라고 하지 말 것):
   - 공통: `pnpm typecheck && pnpm lint && pnpm test`, `git diff --check`.
   - STEP별 추가 검증(아래 "STEP별 게이트").
   - **STEP 스코어카드**를 만든다: 각 성공 기준 pass/fail + 0~100 점수 + 근거(명령 결과 발췌).
D. **이슈 트리아지 & 결정**: 실패나 미흡이 있으면 각 이슈를 분류하고 처리한다.
   - **즉시 수정(fix-now)**: 빌드/테스트 실패, 스펙·계약(openapi.yaml / golden_tests.json) 위반, 보안·개인정보·결제, 데이터 손실/중복, 다음 STEP이 의존하는 기능 결함. → 이 STEP 안에서 고치고 C로 돌아가 재검증.
   - **다음 STEP 반영(defer)**: 순수하게 표면적/사소하거나, 뒤 STEP에서 자연히 해결·대체되거나, 묶어서 처리하는 게 효율적인 폴리시. → PROGRESS.md "Deferred"에 대상 STEP과 이유를 적고 진행.
   - **절대 defer 금지(항상 fix-now)**: 추천 엔진 골든 테스트 실패, 보안/PIPA 결함, 데이터 손실. **테스트를 삭제·약화해서 통과시키지 말 것.**
   - 각 이슈마다 "fix-now / defer + 이유"를 명시한다.
E. **기록**: PROGRESS.md를 갱신한다 — STEP 상태(완료/부분), 스코어카드, 내린 결정(수정/이월), 주요 기술 결정(버전 고정 등), 실행 명령과 결과 요약, 명시한 가정.
F. **커밋**: 의미 있는 메시지로 커밋한다(`feat|chore|test(scope): STEP N ...`).
G. **다음 STEP**: N+1을 시작하기 전에, 이번 STEP을 대상으로 이월된 항목이 있으면 **먼저** 처리한다.

## STEP별 게이트(핵심 추가 검증)
- **STEP 1**: `db:seed` 후 exercises count == 30, 대체(substitutions) 참조 전부 존재, 통합 테스트 통과.
- **STEP 2**: openapi 경로가 api 컨트롤러와 누락 없이 대응, 타입 생성물 빌드 통과, 목서버 기동.
- **STEP 3**: `docs/specs/golden_tests.json` 전 케이스 통과(reason_code 정확 일치, e1rm tolerance). 순수 함수(랜덤/시간 의존 없음).
- **STEP 4**(테스트 스코프, docs/TEST_SCOPE.md): 로그인 보류 → 고정 dev-user 미들웨어 + programs/sessions(+편집). 각 통합 테스트 통과 + openapi 계약 일치, 데이터는 user_id 기준.
- **STEP 5**: 핵심 플로우가 목/실서버로 끝까지 동작, 접근성(대체텍스트·대비) 확인.
- **STEP 6**: E2E로 '오프라인 로깅 → 복귀 → /sync → 서버 추천 반영'을 재현하고 재시도 시 **중복/유실 0**을 단언. "사람 검토 필요" 표시.
- **STEP 7**(테스트 단계): 결제 실연동 대신 **엔타이틀먼트 토글** + 핵심 이벤트 계측. free/pro 토글로 게이팅 변화 확인. (`/billing/*`, `/webhooks/pg`는 만들지 않음.)
- **STEP 8**: 통증 안전 플로우, 브라우저 E2E(iOS PWA 포함), 골든 테스트가 CI 게이트로 동작.

## 사람에게 멈춰 물어야 할 때 (그 외엔 자율 진행)
- 되돌릴 수 없는 변경, 보안/결제/개인정보 설계 판단, 외부 리소스 부재(DB·시크릿 미설정), 같은 게이트를 여러 번(≥3) 시도해도 통과 못 할 때.
- 멈출 때는 "무엇을·왜·시도한 것·후보 선택지"를 정리해 물어라.

## 최종 완성본 다각도 평가 (STEP 8 이후, 목표 ≥ 99/100)
전체가 서면 아래 루브릭으로 **증거 기반** 평가를 하라. 각 항목은 실제 측정/테스트로 뒷받침해야 하며, 근거 없는 자화자찬은 금지다. **스스로에게 가혹한 평가자가 되어라.**

가중치(합 100):
- 기능 완성도·정확성 **25** — PRD/MVP 충족, 골든 전 케이스, 핵심 루프 동작, 오프라인 복원.
- 코드 품질·보안 **15** — 테스트 커버·타입 안정성·보안/PIPA·시크릿 미노출.
- 디자인 **12** — 시각적 완성도·일관성·디자인 토큰.
- UI **12** — 레이아웃·컴포넌트·반응형·빈/에러 상태.
- UX **14** — 온보딩 마찰·한손 로깅·오프라인 회복력·명료성·접근성.
- 실 성능 **12** — 로컬 저장 <200ms, 상호작용 지연, Lighthouse 성능, 버벅임 없음.
- 실 사용성 **10** — 실제 사용자가 핵심 루프를 매끄럽게 완주, 엣지케이스.

**하드 게이트(하나라도 실패면 총점 상한 70)**: 전체 테스트 green, 골든 전 케이스 통과, 보안/PIPA 위반 없음, 시크릿 미노출, 핵심 루프가 오프라인 포함 동작.

**측정 방법(가능한 것은 실제 수행)**:
- 성능: 프로덕션 빌드 + Lighthouse(성능/접근성/베스트프랙티스), 주요 상호작용 시간 측정.
- UX/사용성: 핵심 플로우 E2E(온보딩→프로그램→오프라인 로깅→추천→요약) 실제 통과, 빈/에러/오프라인 상태 확인.
- 접근성: axe 등으로 위반 0 지향.
- 기능: 골든·통합·E2E 결과.

**루프**: 총점 < 99면, 가장 낮은 항목부터 구체적으로 개선하고 재평가하라(개선→재측정 반복).
99 이상 도달 또는 개선이 정체되면, **정직하게** 현재 점수·남은 병목·코드로 넘을 수 없는 사람 결정 필요 항목(디자인 방향·콘텐츠·미디어·법무 등)을 보고하라.
**점수를 조작하지 말 것 — 99는 증거로 증명되어야 한다.** 못 넘으면 이유를 말하라.

## 산출물
- PROGRESS.md에 STEP별 스코어카드와 결정 로그를 누적.
- 최종에 FINAL_EVALUATION.md에 항목별 점수·근거·측정 결과·남은 이슈를 정리.
